import React from 'react';
import AppNavBar from '../../components/AppNavBar';

const ViewLocations = () => {
    return(
        <div>
            <AppNavBar />
            View Locations
        </div>
    );
};

export default ViewLocations;