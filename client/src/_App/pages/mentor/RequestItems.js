import React from 'react';
import AppNavBar from '../../components/AppNavBar';

const RequestItems = () => {
    return(
        <div>
            <AppNavBar />
            Request Items
        </div>
    );
};

export default RequestItems;